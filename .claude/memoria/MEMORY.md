- [✅ RESUELTO: el grosor volvía a 15 mm en Windows](bug-grosor-15mm.md) — causa raíz: `cnc_set()` escribía `cnc_config.json` sin `encoding='utf-8'`; cp1252 (default de Windows) no codifica el `″` de "1/8″" en los nombres de fresa de fábrica, la escritura truncaba el archivo a 0 bytes y `cnc_get()` caía en silencio a los defaults. Arreglado (`encoding='utf-8'` explícito en las 6 lecturas/escrituras de esa config) y probado antes/después en la máquina que fallaba; de paso se encontró y arregló un BOM en `version.txt` que rompía `--diagnostico`. Regla nueva: todo JSON de este proyecto debe leerse/escribirse con `encoding='utf-8'` explícito. **Cerrada la CLASE ENTERA** (COMMIT 8c0e3bc, release `v2026.07.29.6`): los otros **5 sitios** con la misma asimetría (leer sin encoding lo que se escribe en UTF-8) — `plotter_config.json` en `design_studio.py`/`studio_server.py`/`plotter_control.py`, y `version.txt` en `plotter_control.py`/`updater.py` ahora con **`utf-8-sig`**, que **se come el BOM** (en `updater` era grave: `.strip()` no lo quita, la versión salía `﻿2026.…` y `vkey()` no podía compararla). **La regla vive en `CLAUDE.md`**, no solo en la memoria.
- [✅ La rebaba del CNC eran micro-segmentos, no la velocidad](rebaba-microsegmentos.md) — 3-ago-2026: la MISMA pieza cortada con Aspire salió limpia y con Design Studio con rebaba, pese a que el perfil era **geométricamente idéntico** (mismos perímetros, mismas 2 pasadas, mismos sentidos). Causa: `buffer()` de shapely redondea CADA vértice con un arco → en un contorno ondulado (giros ~0.9°) siembra un segmento de ~0.047 mm pegado a cada segmento bueno (**48.7 %** de la trayectoria). A 4000 mm/min ese movimiento dura medio milisegundo, el RichAuto **frena** en cada uno → la máquina tartamudea, el bocado por filo oscila y **la chapa del triplay se desgarra**. Contraintuitivo: en el papel los números de DS eran los CORRECTOS (0.111 mm/filo) y los de Aspire iban casi frotando (0.032) — **un bocado constante vence a uno ideal pero errático**. Arreglo: `_simplify()` (Douglas-Peucker 0.01 mm) en los 3 sitios del offset de `cnc_gcode.py`; 989 micro-segmentos → 0, perímetro cambia 0.0009 %, `auditar_gcode.py` 133 OK. **La regla vive en `CLAUDE.md`.** **+ comparación de RUTA de los dos .tap:** el perfil coincide al centímetro; los **6 m de más** de DS (16.41 vs 10.41 m) son **una pasada de cajeado entera**, porque `_passes()` repartía "pasadas llenas + lo que sobre" (6 mm con pasada de 5 → `[5, 6]`, la segunda quitaba **1 mm** y frotaba) — ✅ **ARREGLADO**, ahora reparte por igual (3+3), mismo recorrido y bocado parejo; ✅ **+ ENCADENADO DE ANILLOS del cajeado** (`_chain_rings`, une anillos contiguos con un movimiento corto a profundidad en vez de retirarse): **67 → 5 trayectorias**, **68 → 9 retiros**, aire 6.90 → 5.06 m sobre el diseño real; la condición de unión es de **SEGURIDAD** (el tramo debe caber en `poly.buffer(-radio)`, o con un cajeado partido en islas se arruina la pieza). **Medido contra Aspire sobre la MISMA zona (185 vs 182 cm²): el cajeado nuevo ya GANA — 9 trayectorias contra 12 y 0.88 m de aire contra 1.06**, recorriendo lo mismo; el margen de aire se agotó. ⏳ Lo único que falta es un AJUSTE DE JOSE, no código: su preset tiene la pasada máxima en 5 mm y por eso parte el cajeado de 6 mm en dos; poniéndola en 6 sería **1.7 min contra los 6.4 de Aspire** (se dejó a su criterio: es un diámetro completo de un tirón, y a 4000 mm/min contra los 1016 de Aspire). **+ "Última pasada separada"**: ataca una causa de rebaba DISTINTA (la flexión de la fresa, no la vibración) y cuesta +50 % de recorrido; su trampa era que el anillo de acabado va a fondo de una vez y **sin rampa se clavaba 17.20 mm en vertical** — ahora marcar la casilla enciende sola la rampa e "Invertir dir." (`v2026.08.04`). Releases: `v2026.08.03`, `v2026.08.03.2`, `v2026.08.04`. ⏳ PENDIENTE: que Jose corte el A/B (hay un `.tap` limpio junto al original, subconjunto exacto, desviación 0.00999 mm) — el diagnóstico está medido sobre la geometría, nadie ha probado aún que la máquina tartamudeara. **RONDA 2 (29-ago-2026, el cocodrilo): la geometría ya NO era el problema** (segmentos <0.2 mm: Aspire 14.0 %, nosotros 1.2 %) — eran (1) **avance irreal**: pedíamos F4000 cuando la máquina necesita 3.7–11 mm solo para acelerar y el segmento típico mide 0.69 mm, así que iba **el 94 % del recorrido acelerando o frenando** (Aspire 57 %) para ahorrar **48 s**; (2) **pasada de 6.10 mm contra 2.44** = todo el filo enterrado, 7× la fuerza (⚠️ vino de un consejo MÍO de agosto); (3) **la rampa de entrada salía al avance de CORTE**, 686 mm/min verticales contra 90. ✅ **ARREGLADO EN CÓDIGO**: `_emit_cut` (entrar al material siempre a `plunge`), `_modal` (salida modal estilo Vectric + fuera 36 movimientos de largo cero: 32.3 → 17.6 caracteres por bloque), `parseTap` de la vista 3D (**veía 68 movimientos de 8812** con el formato nuevo, vacía y sin error), auditor modal con 2 invariantes nuevos, **133 OK**; round-trip sobre el .tap real con desviación **0.000000000 mm**. ⏳ Los **presets** (avance 1500, picada 400, pasada 3.0) son datos vivos de Jose y siguen sin cambiar — **y sin cortar el A/B.**
- [La vista 3D: fiel al recorte, ciega al terminado](render-3d-fidelidad.md) — ⚠️ LEER antes de usar la vista 3D como prueba de nada. Simula el `.tap` REAL (`parseTap` del G-code que exporta, fresa como cilindro del radio real, rejilla adaptativa de 2.4 M celdas ≈ **0.25 mm/celda**), así que es fiel a profundidades, orden, encadenado y pasantes. Pero es un **mapa de alturas**: no modela vibración, rebaba, quemado ni desgarro. **Medido:** el cajeado antes y después de los arreglos da **0 celdas de diferencia de 2.4 M**, y el micro-segmento que causaba la rebaba medía 0.047 mm = **0.18 de una celda** — la vista 3D no enseñó el problema ni enseña la mejora. Sí sirve para cazar un encadenado mal hecho (cortaría un canal visible de 6 mm) y para ver la ruta en la animación. **Lección: una simulación geométrica solo puede fallar en geometría** — pregunta qué magnitud física modela antes de confiar en ella.
- [CNC RichAuto · integración en curso](cnc-richauto.md) — 🚧 CNC 122×244cm (Asia Robotics, RichAuto/AutoNow **A11E** offline por USB) integrada a Design Studio estilo Vectric Aspire, exporta G-code `.tap`. Manual leído (solo G00/G01; trampa "F Read=Ign"). **Fases A–I construidas y COMMITEADAS 22-jul** (A: a1b1dac · B: 9ec1eaf · C: 3a07e92 · D+E+F: 1993945 · G: 4035c5e · H: dba4178 · I: caa5092) + **Vista 3D estilo Aspire R1–R4 + pulido COMMITEADOS 23-jul** (853cb8f · 9506659 · d46dd35 · 5c89b7b · 94868b2: simula el .tap real sobre mapa de alturas, materiales con chapas por shader, hoja=área de trabajo, doble clic quita sobrante, pasantes visibles, animación con fresa) + **auditoría pre-corte `auditar_gcode.py` (79 checks) 24-jul** + **integración A11E COMMIT db5f141 24-jul, PROBADA EN MÁQUINA** (marchas S1-S8 reales por cercanía de RPM, espera de husillo, F Read/S Read/Spindle delay configurados, presets re-validados, Config avanzada; sigue: rebaba MDF 3mm → prueba A/B + downcut, y corte medido — flujo dos pasos Diseño|CNC, rampas Suave/Zigzag/Espiral, A favor/Contra, flechas de sentido en magenta, candado de diseño en paso CNC, alerta de traspaso de grosor, Calcular-y-cerrar, Guardar G-code como paso final, renombrar/nombres arreglados): pestaña CNC estilo Aspire — **flujo: seleccionar → operación → FORMULARIO → "Calcular" (crea/recalcula + auto-preview); NO escribe en vivo**. Lista con ojito/orden ↑↓; Perfil/Cajeado/Taladro; huecos-primero + vecino más próximo; puentes opcionales (Nº/distancia, rampeados); entrada en rampa; **P. inicial, Dirección climb/conv (anillos orientados), Holgura, Z segura, volver-a-0,0**; Render al ancho de fresa; export multi-trabajo (misma fresa); uid estable, .dstudio v2. **PRIMER CORTE REAL HECHO 24-jul: a medida, orientación ✓, husillo OK (la espera de 10s es el Spindle delay del handle; poner campo Husillo de la app en 0)**; sigue: rebaba MDF 3mm (A/B cortador downcut/velocidades); diferido: leads, editor de tabs con clic, V-carve.
- [Design Studio · la interfaz nueva (rebuild)](design-studio.md) — 🆕 App nueva (web/pywebview) que reconstruye la cara de Plotter Antike con look pro, reusando el motor. **Novedades 21-22 jul**: calco de imagen, capas anidadas, selección múltiple, alinear-a, ícono Escritorio. **22-jul: eje Y corregido EN SERIO** (volteo por formato en backend — el volteo ciego de ayer ponía .ai/.dxf de cabeza; ver nota antes de tocar orientaciones), potrace por ruta absoluta (PATH de Finder), y el modo CNC ya no pierde su área al abrir diseños. **23-24 jul**: `.ai` que perdían trazados arreglado en el motor (492b1c8, formato de items de pymupdf) y botón **Importar** (sumar archivos a la mesa sin reemplazar) **COMMITEADO db5f141**. Técnica nueva: probar la UI entera en node con DOM falso + gancho `__DS`. **24-jul: arranca la épica "reemplazar Illustrator"** (formas → texto → booleanas/offset → nodos); formas básicas (R/E/P/L + botón único con flyout) ✅ COMMIT 86765a0; **texto con fuentes del sistema** (`text_vector.py`, modal, doble-clic reedita, .dstudio v3) ✅ COMMIT c5c34d4 (14+23 checks + e2e); **booleanas Unir/Restar/Intersectar + contorno-offset** (`geo_ops.py`) ✅ COMMIT c9d2434; **edición de nodos** (`curve_fit.py` Schneider re-ajusta Béziers, flecha hueca A, arrastrar anclas/manijas, doble clic agrega, Supr quita, Esc hornea) ✅ COMMIT e856837 — la ÉPICA v1 COMPLETA y PUSHEADA (24-jul); **rondas 1+2 (9 funciones más: texto en arco, pluma P, tijeras C, imanes, reglas/guías, copias en círculo, engrosar línea, esquinas redondas, imagen de referencia)** ✅ COMMIT 7279562 y PUSHEADAS (24-jul); **menú de clic derecho** (editar texto/nodos, cortar aquí, booleanas, contorno, z-order Traer al frente/Enviar atrás NUEVO, pegar en vacío, Acomodo completo, Restablecer transformación, Bloquear/Desbloquear NUEVO) + limpieza de barras (Ajustar-vista en píldora, fuera del riel calcar/espejo/ver-vectores, fuera del panel Acciones rápidas y Voltear, arranque siempre en paso Diseño) + Cmd+Y modo contorno / rehacer solo Cmd+Shift+Z ✅ COMMIT b005939 y PUSHEADO (24-jul); **fuentes: caché a disco (2.9s→0.001s) + calentado al arrancar + selector visual con cada fuente dibujada en sí misma y buscador, flechas ↑/↓ con vista previa en vivo, desplegable sin decapitar** ✅ COMMITS f73dbb9 + c38d4e7, PUSHEADOS (24-jul); **NESTING** (`nest_ops.py` BLF+rotaciones+compactación sobre shapely, bloqueadas=obstáculos, sección "Acomodar en la hoja", 30 piezas/0.65s) + **rotación libre con el mouse** ✅ COMMIT 423f65f y PUSHEADOS (24-jul; nesting AUTOMÁTICO 90/45/15 multi-arranque) + **animación EN VIVO (hilo+sondeo) y MODO ROLLO plotter (845→120mm, 7× menos vinil; used en cm)** ✅ COMMIT 90161c3 y PUSHEADO; **escalar arrastrando esquinas (Shift=proporcional) + autoguardado cada 60s con recuperación al abrir** + **Suavizar facetas por clic derecho (costuras con tangente central 15°→2.6°)** ✅ COMMIT 446d9ce y PUSHEADOS (24-jul, regresión 228); **tanda reflejos Illustrator: Cmd+A, Alt+arrastre copia, Transformar-de-nuevo ⇧⌘D (patrones), Exclusión+Dividir, unidades mm|pulgadas, multi-hoja CNC con nesting que reparte y Traer-hoja-aquí** ✅ COMMIT 1683124 y PUSHEADA (24-jul, regresión 246); **COTIZADOR** (botón en barra: piezas+corte+tiempo+material→desglose con margen y precio c/u, tarifas persistidas por máquina, Copiar para WhatsApp) construido y verificado (9 checks al centavo); **ESQUINAS VIVAS estilo Illustrator** (widget junto a cada esquina recta en el editor de nodos, radio en vivo con medida, multi-esquina, arco exacto r·(√2−1) verificado) + reacomodo de UI (Esquinas del panel fuera, medidas a la barra inferior, nesting al riel con desplegable) ✅ COMMIT d6c6b77 y PUSHEADOS (24-jul, regresión 261). **27-jul: CNC — clic en una trayectoria la selecciona en la lista (y clic fuera la suelta); la elegida se pinta VERDE — regla de colores del lienzo: magenta=una · azul=varias · verde=trayectoria activa** ✅ COMMITS 8dfc5ba+477a802+26a8a52. **27-jul: barras reacomodadas** (Cotizar a la barra inferior como icono; fuera el botón Exportar HPGL) ✅ COMMITS 7ccc539+28ce65b. **27-jul: EXPORTAR COMO…** (`export_ops.py`: PNG/JPG/PDF/SVG/DXF sin dependencias nuevas; PNG/JPG salen de rasterizar el mismo PDF) ✅ COMMIT 0d8bed9. **27-jul: VISTA RELLENA** (botón en el RIEL izquierdo + Cmd+Shift+Y; sólida y del color del contorno; par-impar = lo relleno es lo que queda de material) ✅ COMMIT b37520c. **25-jul: EMPAQUETADO Win+Mac + AUTO-ACTUALIZACIÓN** (`updater.py`: descarga con avance y ayudante externo que reemplaza la app y la relanza; potrace viaja dentro; `--diagnostico`; CI reescrita — solo se distribuye Design Studio) ✅ COMMIT d77d07c; **RELEASES PUBLICADOS 27-jul: v2026.07.27 y v2026.07.27.2** (Win 64 MB · Mac 57 MB) — ⚠️ la consola de Windows no imprime `✓`: salida de terminal en ASCII. **28-jul: la app NO ARRANCABA en Windows** — Windows marca como "de internet" lo que sale de un zip del navegador y .NET se NIEGA a cargar el DLL de pythonnet (sin él pywebview no dibuja ventana); el instalador ahora hace `Unblock-File`, y `--diagnostico` por fin comprueba **la ventana** (antes no: se carga de forma perezosa, o sea después del chequeo → la CI publicaba "todo completo" una app que moría al abrirse) ✅ COMMIT 55ee5ce, **RELEASE `v2026.07.28`** (confirmado: ya abre). **28-jul, acto II: se colgaba ~2 de cada 3 arranques** — el calentado de fuentes salía ANTES de `create_window` y le peleaba el intérprete a la ventana mientras nacía (Python corre un hilo a la vez); en Mac invisible (3 s) y en Windows fatal (~1 min por el antivirus); ahora va en `events.loaded` y suelta el intérprete cada 16 archivos ✅ COMMIT 8ea6acd, **RELEASE `v2026.07.28.2`** — **regla: un fallo INTERMITENTE casi nunca es algo roto, son dos cosas compitiendo; y ver la interfaz NO prueba que la app viva (WebView2 pinta en otro proceso)**. **29-jul: CAUSA RAÍZ del cuelgue, y era nuestra** — `api.window = win` colgaba la ventana del objeto `js_api`, y pywebview RECORRE ese objeto para exponer sus métodos: se metía dentro de la Window y tocaba `width`, cuyo getter le pide el tamaño a la ventana nativa **que aún se estaba creando en otro hilo** → abrazo mortal; arreglo = renombrar a `_window` (pywebview salta los `_`) ✅ COMMIT 72e3ec4, **RELEASE `v2026.07.29.2`**. Lo delató **instrumentar** (`v2026.07.29`: `arranque.log` + vigilante `faulthandler` que vuelca las pilas si el arranque pasa de 25 s — **no quitarlo, la app va sin consola**) tras CINCO teorías caídas razonando a ciegas. **Reglas: nunca cuelgues del objeto `js_api` un atributo público que sea un objeto; y en cuanto un fallo viva solo en una máquina que no puedes tocar, deja de deducir e INSTRUMENTA.** ✅ **CONFIRMADO en la máquina: 5 arranques seguidos limpios** (con la tasa vieja, 1 entre 243 por azar). **29-jul: fabricante renombrado a `BuiltByJose` y config a `DesignStudio`** (antes `Antike\PlotterController`, heredado de la app vieja); la app **COPIA** la config vieja al arrancar (`_migrar_config` — copia y NO mueve: ahí viven las fresas del CNC) y el instalador borra la instalación vieja con tres guardas ✅ COMMIT 3ace6e7 · **elegir la máquina CNC ya NO salta el panel al paso CNC**, se pasa a mano con el toggle ✅ COMMIT d7faf36 — **RELEASE `v2026.07.29.3`**. **PENDIENTE**: verificar corte en hardware (eje Y del plotter), que Jose pruebe el ciclo de actualización, probar los 12 diálogos de archivo renombrados (Abrir/Guardar/Exportar) en Windows, y probar en la máquina el renombre + el paso CNC.
- [Idea: Design Studio como producto](idea-comercial.md) — 💡 evaluada 24-jul: el combo diseño+plotter+CNC NO existe en el mercado (LightBurn lo hizo para láser); plan por pasos: producción propia (✅ en curso) → piloto 2-3 talleres → nicho de pago; muro real = diversidad de máquinas; ⚠️ repo público, decidir antes de vender.
- [Idea: Design Studio web/online](idea-web-online.md) — 💡 EN PAUSA: app web local (servidor `studio_server.py`) para usar Design Studio desde varios dispositivos sin instalar nada. Prototipo funcional. Candado: cortar necesita el USB local. Se guardó para retomar.
- [Plotter Antike · ESTADO](estado.md) — 👉 LEER PRIMERO: app de escritorio Python/tkinter que controla un plotter de corte por COM/USB (envía HPGL); abre SVG/DXF/AI; corre en Win/Mac/Linux, se distribuye compilada con auto-update desde GitHub. Rescatada 21-jul-2026; último trabajo real 25-may-2026, dormida desde entonces. CLAUDE.md **auditado y reescrito el 21-jul-2026** contra el código (el viejo describía una versión fantasma: smooth=True→False, CutCanvas y _pinch_corners muertos, UI distinta) → ya es de fiar.
- [Tipografías: Google Fonts instaladas y el selector por familia](fuentes-google.md) — las fuentes salen del SISTEMA OPERATIVO, no de la app (`_font_dirs()`); 18-ago-2026 se instalaron las 200 familias más usadas de Google Fonts que faltaban (556 archivos, 117 MB → **413 → 969 fuentes**, 343 familias) y se rehizo el selector: **una fila por familia y dibujado por tandas de 60**, porque cada fila estrena una tipografía y pintarlas todas cuelga el menú. NO se instaló el catálogo entero (1,942 familias, 1.3 GB): las Display/manuscritas no cortan bien y un listado de miles estorba. 27 checks en node con el JS real.
- [El plotter "no aparece": primero el sistema, luego el programa](plotter-puerto-usb.md) — el plotter es un **FTDI FT232R** (`/dev/cu.usbserial-AO004NFH` en la Mac, sin driver que instalar). 18-ago-2026: no lo detectaba y NO era la app — el árbol USB del kernel estaba **vacío**; al reconectar el cable apareció a las 19:42:49 (un vigía muestreando cada 2 s lo probó con hora exacta). Orden de diagnóstico: ¿lo ve el SO? → ¿refrescaste la lista? → ¿falta driver? A raíz de eso: puertos con **nombre legible**, agrupados en "Cables USB" vs "Otros" por **VID/PID** (los internos vienen vacíos y nunca son plotter), autoselección del único cable y **memoria del último puerto** (COMMITS 41afada, fa2b5c9).
